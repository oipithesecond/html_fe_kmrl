# Kochi Metro AI Train Induction Planner

## Setup
1. Install Node.js dependencies: `npm install`
2. Run server: `npm start`
3. Access at: `http://localhost:3000`

## Requirements
- Python 3.8+
- OR-Tools installed: `pip install ortools`